import React from 'react';
import { motion } from 'motion/react';
import { Briefcase, ShoppingBag, Search, ArrowRight, Plus } from 'lucide-react';
import { useAuth } from '../contexts/AuthContext';

interface DashboardProps {
  onAction: (tab: string) => void;
}

export const Dashboard: React.FC<DashboardProps> = ({ onAction }) => {
  const { profile, user } = useAuth();

  const cards = [
    {
      id: 'jobs',
      title: 'Create Job',
      description: 'Hire skilled comrades for coding, tutoring, or errands.',
      icon: Briefcase,
      color: 'blue',
      stats: '124 Gigs Active',
      gradient: 'from-blue-500/10 to-transparent',
      borderColor: 'border-blue-500/20',
      iconBg: 'bg-blue-500/20',
      iconBorder: 'border-blue-400/30',
      iconColor: 'text-blue-400',
      tagColor: 'text-blue-400 bg-blue-400/10',
      btnColor: 'bg-blue-500',
    },
    {
      id: 'marketplace',
      title: 'Build Shop',
      description: 'Sell electronics, snacks, or hostel essentials instantly.',
      icon: ShoppingBag,
      color: 'emerald',
      stats: '89 Items Sold',
      gradient: 'from-emerald-500/10 to-transparent',
      borderColor: 'border-emerald-500/20',
      iconBg: 'bg-emerald-500/20',
      iconBorder: 'border-emerald-400/30',
      iconColor: 'text-emerald-400',
      tagColor: 'text-emerald-400 bg-emerald-400/10',
      btnColor: 'bg-emerald-500',
    },
    {
      id: 'discover',
      title: 'Discover',
      description: 'Browse trending listings across all Kenyan campuses.',
      icon: Search,
      color: 'purple',
      stats: 'New Drops Today',
      gradient: 'from-purple-500/10 to-transparent',
      borderColor: 'border-purple-500/20',
      iconBg: 'bg-purple-500/20',
      iconBorder: 'border-purple-400/30',
      iconColor: 'text-purple-400',
      tagColor: 'text-purple-400 bg-purple-400/10',
      btnColor: 'bg-purple-500',
    },
  ];

  return (
    <div className="space-y-10">
      <header>
        <h1 className="text-4xl font-extrabold text-white tracking-tight leading-none">
          Good {new Date().getHours() < 12 ? 'Morning' : 'Afternoon'}, <br />
          <span className="text-transparent bg-clip-text bg-gradient-to-r from-white to-slate-500">
            {profile?.username || user?.displayName?.split(' ')[0] || 'Comrade'}
          </span>
        </h1>
        <p className="text-slate-400 mt-3 text-sm">Manage your campus economy. 4 pending notifications today.</p>
      </header>

      <div className="grid gap-6">
        {cards.map((card, index) => (
          <motion.div
            key={card.id}
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: index * 0.1, duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
            onClick={() => onAction(card.id)}
            className={`group block cursor-pointer bg-gradient-to-b ${card.gradient} border ${card.borderColor} rounded-[2rem] p-6 transition-all duration-500 hover:scale-[1.02] hover:-translate-y-1 active:scale-95`}
          >
            <div className={`w-12 h-12 ${card.iconBg} rounded-2xl flex items-center justify-center mb-4 border ${card.iconBorder} transition-transform duration-500 group-hover:rotate-6 group-hover:scale-110`}>
              <card.icon className={card.iconColor} size={24} />
            </div>
            
            <h3 className="text-xl font-bold text-white mb-2">{card.title}</h3>
            <p className="text-[13px] text-slate-400 mb-6 leading-relaxed">
              {card.description}
            </p>

            <div className="flex items-center justify-between">
              <span className={`text-[10px] font-black uppercase tracking-widest ${card.tagColor} px-3 py-1 rounded-full border border-white/5`}>
                {card.stats}
              </span>
              <div className={`w-10 h-10 rounded-full ${card.btnColor} flex items-center justify-center text-white shadow-lg transition-all duration-300 group-hover:rotate-90`}>
                {card.id === 'discover' ? <ArrowRight size={18} /> : <Plus size={18} />}
              </div>
            </div>
          </motion.div>
        ))}
      </div>

      {/* Simplified Trending Feed Preview */}
      <div className="space-y-4">
        <div className="flex items-center justify-between">
          <h2 className="text-lg font-bold text-white">Trending Feed <span className="text-[10px] font-bold text-slate-500 ml-2 uppercase tracking-widest">JKUAT MAIN</span></h2>
          <button className="text-[10px] font-black text-cyan-400 uppercase tracking-widest" onClick={() => onAction('discover')}>View All</button>
        </div>
        
        <div className="bg-[#111114] border border-white/5 rounded-3xl p-4 flex gap-4">
          <div className="w-16 h-16 bg-slate-800 rounded-2xl flex-shrink-0 animate-pulse"></div>
          <div className="flex flex-col justify-between py-1">
            <div>
              <span className="text-[9px] uppercase tracking-tighter bg-amber-500/10 text-amber-500 px-2 py-0.5 rounded-full font-black border border-amber-500/20">Gig</span>
              <h4 className="text-sm font-bold text-white mt-1">React Dashboard Fix</h4>
            </div>
            <p className="text-xs text-emerald-400 font-mono font-bold">KES 2,500 <span className="text-slate-500 font-normal ml-1">· Escrow Ready</span></p>
          </div>
        </div>
      </div>
    </div>
  );
};
